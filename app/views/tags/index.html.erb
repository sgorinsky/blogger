<h1> All Tags </h1>

<ul>
  <% @tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag) %>
    -
    <%= link_to "(delete)", tag_path(tag), method: :delete, data: {confirm: "Delete tag?"} %>
    <br>
  <% end %>

</ul>
